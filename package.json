{
  "name": "sports-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Sports Tracker - Multi-Sport Dashboard",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter @sports-tracker/web dev",
    "dev:web": "pnpm --filter @sports-tracker/web dev",
    "build": "pnpm -r build",
    "build:types": "pnpm --filter @sports-tracker/types build",
    "build:web": "pnpm --filter @sports-tracker/web build",
    "test": "pnpm -r test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Bbeierle12/Sports-Tracker.git"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@vercel/node": "^3.2.0"
  }
}
